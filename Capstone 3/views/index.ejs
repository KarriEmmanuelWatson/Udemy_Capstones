<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>All Posts</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-pink-100 to-orange-100 min-h-screen font-sans text-gray-800">
  <div class="max-w-3xl mx-auto p-6 mt-10 bg-white shadow-2xl rounded-xl">

    <h1 class="text-4xl font-bold text-center text-transparent bg-clip-text bg-gradient-to-r from-pink-500 to-orange-400 mb-8">
      📚 All Posts
    </h1>

    <div class="flex justify-center mb-6">
      <a href="/create" class="px-6 py-2 bg-green-500 text-white font-semibold rounded-lg hover:bg-green-600 transition">
        + New Post
      </a>
    </div>

    <% if (posts.length === 0) { %>
      <p class="text-center text-gray-500">No posts yet.</p>
    <% } else { %>
      <% posts.forEach(post => { %>
        <div class="bg-orange-50 border border-orange-200 rounded-lg p-4 mb-5 shadow-sm hover:shadow-md transition">
          <h2 class="text-xl font-semibold text-orange-900 mb-2"><%= post.title %></h2>
          <p class="text-gray-700 mb-4"><%= post.content %></p>
          <div class="flex space-x-3">
            <a href="/edit/<%= post.id %>" class="px-3 py-1 text-sm bg-yellow-400 text-white rounded hover:bg-yellow-500 transition">Edit</a>
            <form method="POST" action="/delete/<%= post.id %>" onsubmit="return confirm('Delete this post?')">
              <button class="px-3 py-1 text-sm bg-red-500 text-white rounded hover:bg-red-600 transition">Delete</button>
            </form>
          </div>
        </div>
      <% }) %>
    <% } %>
  </div>
</body>
</html>
